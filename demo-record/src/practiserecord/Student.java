public record Student (String lastName, String firstName, int id) {
  // built-in All Arguments Constructor
  // getters .lastName(), firstName(), id()
  // .equals()
  // .hashCode()
  // .toString()
}

